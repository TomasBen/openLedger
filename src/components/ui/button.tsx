import { Button } from "@mantine/core";

export function CustomButton(){
  return (
    <Button style={{ backgroundColor: 'var(--color-primary-container)', color: 'var(--color-on-primary-container)' }}>
    </Button>
  )
}
