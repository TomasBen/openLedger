export default function Dashboard(){
  return (
    <button style={{ height: "50px", width: "auto", margin: "auto" }} onClick={() => console.log("this works")}>
      this is a button in the center
    </button>
  )
}
